import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 5,
    margin: 10,
    backgroundColor: "#F03A73",
    borderRadius: 10,
    alignItems: "center",
  },
  title: {
    fontWeight: 'bold',
    fontSize: 18,
    color: 'white',
  },
});

export default styles;
